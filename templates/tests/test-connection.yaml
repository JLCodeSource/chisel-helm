apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chisel-helm.fullname" . }}-test-connection"
  labels:
{{ include "chisel-helm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chisel-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
